---
export interface Props {
  white?: boolean;
  noCenter?: boolean;
  noMarginBottom?: boolean;
  centerOnMobile?: boolean;
  md?: boolean;
}

const { white, noCenter, noMarginBottom, centerOnMobile, md } = Astro.props;
const mb = noMarginBottom ? "" : white ? "mb-2" : "mb-4 md:mb-8";
const textSize = md
  ? "text-5xl"
  : white
    ? "text-2xl md:text-6xl"
    : "text-2xl md:text-5xl";
---

<h2
  class={`uppercase font-extrabold ${textSize} ${white ? "text-white " : "text-primary "} ${mb} w-full ${noCenter ? "" : "text-center"} ${centerOnMobile ? "max-md:text-center" : ""}`}
>
  <slot />
</h2>
