---
export interface Props {
  placeholder?: string;
}

const { placeholder } = Astro.props;
---

<input
  placeholder={placeholder}
  class="w-full max-w-[530px] px-4 py-4 text-xl border-2 rounded-xl border-white text-white mr-4 bg-[rgba(0,0,0,0.5)] outline-none"
/>
